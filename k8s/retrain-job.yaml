apiVersion: batch/v1
kind: Job
metadata:
  name: retrain-small-model
spec:
  backoffLimit: 2
  template:
    spec:
      containers:
        - name: retrain-small
          image: ml-gateway/small:latest
          command: ["python", "train.py"]
      restartPolicy: Never
